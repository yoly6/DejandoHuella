@model IEnumerable<Carrito_de_compras.Models.ModoPago>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ModoPagoDescripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ModoPagoDescripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ModoPagoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ModoPagoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ModoPagoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
