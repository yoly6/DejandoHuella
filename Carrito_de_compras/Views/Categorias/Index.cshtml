@model IEnumerable<Carrito_de_compras.Models.Categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Categoria</h1>
<br />
<p>
    <a asp-action="Create" class="btn btn-primary">Crear</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoriaNombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FacturaDescripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoriaNombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FacturaDescripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CategoriaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CategoriaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CategoriaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
